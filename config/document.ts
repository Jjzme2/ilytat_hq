export enum DocumentType {
    CONTRACT = 'contract',
    PROPOSAL = 'proposal',
    INVOICE = 'invoice',
    BRIEF = 'brief',
    TEMPLATE = 'template',
    NOTE = 'note',
    REPORT = 'report',
    OTHER = 'other'
}

export enum DocumentStatus {
    DRAFT = 'draft',
    REVIEW = 'review',
    FINAL = 'final',
    ARCHIVED = 'archived',
    PUBLISHED = 'published'
}
